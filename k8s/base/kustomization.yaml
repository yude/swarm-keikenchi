apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keikenchi-map

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: keikenchi-map
  app.kubernetes.io/part-of: keikenchi-map
